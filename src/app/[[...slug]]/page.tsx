import LandingPage from '@/views/LandingPage'

/**
 * Marketing site at `/` — full landing with hero + sections below.
 * Dashboard SPA routes can be added here when needed.
 */
export default function CatchAllPage() {
  return <LandingPage />
}
